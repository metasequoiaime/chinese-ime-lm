# 安全

## 威胁模型

这个项目里真正的攻击面是**模型文件**。它是一个会被输入法加载的二进制，而输入法看得见用户的每一次按键。如果一个模型文件能让加载器越界读写，那就是从"下载了一个模型"到"任意代码执行"的一条路。

参考实现按这个前提写：

- 整个 crate `unsafe_code = "forbid"`，没有裸指针、没有手工内存操作。
- 头部长度、每个张量的 `data_offsets` 都用带边界检查的切片取，越界返回错误而不是 panic 或读到别处。
- 加载后校验几何：每个张量的元素数必须与配置推导出的形状一致，词表长度必须与 `config.vocab` 一致，通道数必须能整除注意力头数。不一致就拒绝加载。
- int8 的 scale 数量必须与行数一致。

**但格式本身不提供真实性保证。** safetensors 没有签名。分发方必须按长度和摘要校验模型文件——本项目的 `export.py` 会打印这样一条资源锁条目，正是为此。**不要加载来源不可信、且没有经过摘要校验的模型文件。**

校验工具就在仓库里，除本 crate 外不依赖任何东西：

```sh
cd reference
cargo run --release --example verify -- <模型文件> --expect-sha256 <摘要> --expect-size <字节数>
```

它核对摘要与体积、按参考实现加载（几何一致性、张量偏移、词表长度都在这一步检查）、确认许可与语料署名，最后做一次排序检查——一个被改坏的前向计算仍然能加载成功、元数据仍然完好、耗时看着还很漂亮。本仓库每次发布都会对全部附件跑这个工具，见 `.github/workflows/release.yml`。

## 报告漏洞

请使用 GitHub 的私密漏洞报告（仓库页面 → Security → Report a vulnerability），不要开公开 issue。

报告请包含：能触发问题的模型文件或构造方法、观察到的行为、以及你使用的加载器实现（参考实现还是你自己的）。

如果问题出在你自己按 `docs/format.md` 写的加载器里，而文档没有说清某个必须做的检查，那也算这个项目的缺陷，同样欢迎报告。

## 不属于安全问题的

- **模型给出错误的候选。** 那是质量问题，开普通 issue，最好附评测数字。
- **模型来自网络语料因此可能复述语料里的内容。** 这是字级语言模型的固有性质，不是缺陷。它只在候选之间打分，不生成自由文本。
- **训练管线下载的第三方语料本身的内容。** 语料来源和许可列在 `docs/neural.md` 与 `NOTICE` 中。
